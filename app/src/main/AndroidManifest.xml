<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.slifers.sugar.sugarorm"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        >
        <meta-data
            android:name="DATABASE"
            android:value="example1.db"/>
        <meta-data
            android:name="VERSION"
            android:value="1"/>
        <meta-data
            android:name="QUERY_LOG"
            android:value="true"/>
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.slifers.sugar"/>

        <activity android:name="com.slifers.sugar.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.slifers.sugar.Activity.ListPersonActivity">
        </activity>
        <activity android:name="com.slifers.sugar.Activity.AddPersonActivity">
        </activity>
        <activity android:name="com.slifers.sugar.Activity.PersonDetailActivity">
        </activity>
    </application>

</manifest>
